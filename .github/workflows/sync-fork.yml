name: Sync Fork
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
jobs:
  sync:
    uses: mchristoffers/workflows/.github/workflows/sync-fork.yml@main
    with:
      upstream: "openclaw/openclaw"
      branch: "main"
